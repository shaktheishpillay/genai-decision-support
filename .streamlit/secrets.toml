OPENAI_API_KEY = "your_openai_key_here"
```

4. **Replace `your_openai_key_here`** with your actual OpenAI key
5. Save (Cmd + S)

---

### **1.3: Update `.gitignore` to protect secrets**

**In VS Code:**
1. Open `.gitignore` again
2. Add this line at the bottom:
```
# Streamlit secrets
.streamlit/secrets.toml